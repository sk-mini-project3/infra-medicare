# medical-service-infra (scaffold)

This repo contains Terraform and k8s manifests used by Argo CD.

Edit `terraform` variables and run `terraform init` / `plan` / `apply` after setting `TERRAFORM_STATE_BUCKET`.
