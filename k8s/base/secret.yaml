apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: default
  labels:
    app: medical-service
type: Opaque
stringData:
  # 실제 배포 시에는 terraform outputs에서 가져온 RDS 정보를 사용
  DB_USERNAME: "admin"
  DB_PASSWORD: "ChangeMe123!" # ⚠️ 프로덕션에서는 변경 필수!
  DATABASE_URL: "postgres://admin:ChangeMe123!@medical-service-db.cluster-xxxx.ap-northeast-2.rds.amazonaws.com:5432/medicalservicedb"
